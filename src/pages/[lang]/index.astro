---
const { lang } = Astro.params;
const pages = import.meta.glob('../../content/*/index.mdx');
const getPage = pages[`../../content/${lang}/index.mdx`];
if (!getPage) {
  throw new Error(`Page not found for lang ${lang}`);
}
const { default: Page } = await getPage();
---
<Page />
